ѕроблемы, с которыми € столкнулс€ во врем€ выполнени€ задани€:
1. Ќе отрисовывались GeoJSON-данные. я пробовал добавл€ть Point и LineString, но на карте их не было видно. — этой проблемой € так и не смог разобратс€.
2. ѕри попытке отображени€ GeoJSON-данных, которые записаны в несолько строк, возникала ошибка в Javascript (Syntax Error) - такие строки не получалось преобразовать в Javascript-объект (€ использовал дл€ этого метод JSON.parse()). — этой проблемой тоже не разобралс€.